name: Mor'ia 9.31 Dialogue + Tracker PT-BR
on:
  push:
    branches: [moria-9.28-character-i18n-ptbr]
    paths:
      - 'tools/upgrade-moria-9-31-dialogue-tracker-ptbr.py'
      - '.github/workflows/moria-9-31-dialogue-tracker-ptbr.yml'
permissions:
  contents: write
jobs:
  upgrade:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          ref: moria-9.28-character-i18n-ptbr
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply Mor'ia 9.31 dialogue and tracker pass
        run: python tools/upgrade-moria-9-31-dialogue-tracker-ptbr.py
      - name: Verify upgrade anchors
        run: |
          grep -q "tr(dialogue.text)" src/components/DialogBox.tsx
          grep -q "tr(opt.text)" src/components/DialogBox.tsx
          grep -q "tr(objective.targetName)" src/components/ActiveQuestTracker.tsx
      - name: Verify PT-BR source coverage
        run: python tools/audit-moria-9-28-i18n.py
      - name: Validate client
        run: |
          npm ci
          npm audit --audit-level=high
          npm run typecheck
          npm run build
          bytes=$(wc -c < src/components/GameScreen.tsx)
          echo "GameScreen bytes: $bytes"
          test "$bytes" -lt 170000
      - name: Validate server
        run: |
          npm ci --prefix server
          npm audit --prefix server --audit-level=high
          npm run check --prefix server
          npm test --prefix server
      - name: Commit validated Mor'ia 9.31 pass
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/i18n/pt-BR.928.json src/components/DialogBox.tsx src/components/ActiveQuestTracker.tsx
          if git diff --cached --quiet; then exit 0; fi
          git commit -m 'feat(9.31): localize npc dialogue and active quest tracker'
          git push origin HEAD:moria-9.28-character-i18n-ptbr
