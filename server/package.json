{
  "name": "moria-mmo-server",
  "version": "9.26.1",
  "description": "Production authoritative MMO server for Mor'ia with secure sessions, world authority and persistent quest state",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/*.test.mjs",
    "check": "node --check server.js && for f in engine/*.mjs; do node --check \"$f\"; done && for f in tools/*.mjs; do node --check \"$f\"; done",
    "migrate:legacy": "node tools/migrate-legacy-character.mjs",
    "build": "cd .. && npm install && npm run build",
    "build:start": "npm run build && npm start",
    "tunnel": "npx localtunnel --port 3000"
  },
  "dependencies": {
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
